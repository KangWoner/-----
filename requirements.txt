# 중학생 수학 학습 도우미 "쏘미" - 필수 패키지 목록
# 이 파일은 프로젝트 실행에 필요한 Python 패키지들을 나열합니다

# Flask: 웹 애플리케이션을 만들기 위한 가볍고 쉬운 프레임워크
# - 웹 서버를 실행하고, URL을 관리하고, HTML을 렌더링합니다
Flask==3.0.0

# python-dotenv: .env 파일에서 환경변수를 읽어오는 패키지
# - API 키 같은 민감한 정보를 안전하게 관리합니다
# - .env 파일에 저장된 값을 자동으로 읽어옵니다
python-dotenv==1.0.0

# anthropic: Claude API를 쉽게 사용할 수 있게 해주는 공식 라이브러리
# - AI와 대화하고, 응답을 받는 기능을 제공합니다
# - Anthropic 회사에서 직접 만든 안전한 라이브러리입니다
anthropic==0.39.0

# Werkzeug: Flask의 핵심 도구 (Flask 설치 시 자동으로 설치됨)
# - 웹 서버 기능, 보안 기능 등을 제공합니다
Werkzeug==3.0.1

# ===== 나중에 추가할 패키지들 (지금은 주석 처리) =====
#
# Flask-CORS==4.0.0
# # - 다른 도메인에서 API 호출을 허용할 때 필요 (나중에 배포 시)
#
# gunicorn==21.2.0
# # - 실제 서비스 배포 시 사용하는 안정적인 웹 서버 (개발 중에는 불필요)
